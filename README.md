# RedisBrowser

Start the service:
``` bash
uvicorn --port 3456 main:app
```

Start the DEV dashboard:
``` bash
watch -c -n 1 ./dredis
```

Start the DEV dashboard:
``` bash
watch -c -n 1 ./predis
```